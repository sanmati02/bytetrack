<!DOCTYPE html>
<html>
  <head>
    <title>ByteTrack</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <%= stylesheet_link_tag 'application', 'media' => 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg bg-info text-white mb-3">
      <% if current_user %>
        <%= link_to jobs_path, class: 'title navbar-brand', style: 'color: #fff;' do %>
          <h1>ByteTrack</h1>
        <% end %>
      <% else %>
        <%= link_to root_path, class: 'title navbar-brand', style: 'color: #fff;' do %>
          <h1>ByteTrack</h1>
        <% end %>
      <% end%>
      <% if current_user %>
          <div class="ml-auto">
              <class="nav-item"> <%= link_to 'Add New Job', new_job_path, class: 'btn btn-light mr-2' %>
              <class="nav-item"> <%= link_to 'See Statistics', stats_path, class: 'btn btn-light mr-2' %>
              <class="nav-item"> <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: 'btn btn-outline-light mr-2' %>
          </div>
      <% end %>
    </nav>
    <div class="container">
      <%= render 'layouts/flash' %>
      <main id="main">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
